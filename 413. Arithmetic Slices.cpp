class Solution2 {
public:
    int numberOfArithmeticSlices(vector<int>& A) {
        int len = A.size();
        if(len <= 2)	return 0;
        int count(0), delta, start(0), end(1);
        
    }
};